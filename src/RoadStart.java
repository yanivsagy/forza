import processing.core.PImage;

import java.util.List;

public class RoadStart extends NonMovingObstacle {
    public RoadStart (String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
